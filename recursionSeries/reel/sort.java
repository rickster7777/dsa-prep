package reel;
import java.util.*;
public class sort {

    public static void main(String[] args) {
        int[] numbers = { 10, 2, 3, 1 };
        Arrays.sort(numbers);
        System.out.println(Arrays.toString(numbers));

        // [1, 2, 3, 10]
    }
}